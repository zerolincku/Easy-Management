<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linck.management.system.mapper.SysRolePermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.linck.management.system.entity.SysRolePermission">
        <id column="id" property="id"/>
        <result column="r_id" property="rId"/>
        <result column="p_id" property="pId"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, r_id, p_id
    </sql>

    <insert id="insertList" parameterType="java.util.List" useGeneratedKeys="false">
        insert into sys_role_permission (r_id,p_id) values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.rId},
            #{item.pId}
            )
        </foreach>
    </insert>
</mapper>
